# Core ML libraries (Inference only)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.21.0

# API and web
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.5

# Data processing
numpy>=1.21.0
pandas>=1.5.0
scikit-learn>=1.2.0
pillow>=9.0.0
opencv-python-headless>=4.7.0
albumentations>=1.3.0

# Monitoring
prometheus-client>=0.16.0

# MLflow (for model loading if needed, but usually strictly not needed if loading from local file, but good to have for logging)
mlflow>=2.0.0
