apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: MLflow
    type: grafana-simple-json-datasource
    access: proxy
    url: http://mlflow-server:5000
    editable: true
